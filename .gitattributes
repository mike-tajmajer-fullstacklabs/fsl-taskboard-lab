# Shell scripts and git hooks must stay LF: a CRLF hook fails closed under sh
# (blocks every commit with a cryptic "command not found"), and Windows
# checkouts with core.autocrlf=true would otherwise convert them.
*.sh text eol=lf
.husky/** text eol=lf
